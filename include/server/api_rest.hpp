#ifndef API_REST_HPP
#define API_REST_HPP

#include "config.hpp"

/**
 * Entry point for the REST API child process.
 */
void run_api_rest_process(const config::server_config& cfg);

#endif // API_REST_HPP
